@require './_variable.styl';

canvas
    display: block;

iframe
    display: block;

.cupo
    cursor: pointer;

.hoda
    &:hover
        position: relative;

        &::after
            content: '';
            display: block;
            position: absolute;
            width: 100%; height: 100%;
            top: 0; left: 0;
            background-color: rgba(0, 0, 0, 0.05)
